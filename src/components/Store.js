import React from "react";
import Header from "./Header";

const Store = (props) => {
  return (
    <div className={"content"} id={"store"}>
      <Header />
      Hello from Store
    </div>
  );
};

export default Store;
